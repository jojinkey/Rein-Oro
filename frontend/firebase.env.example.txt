# Copy these values into frontend/.env.local for local development.
# Add the same VITE_* variables to Netlify/Vercel for production builds.

VITE_FIREBASE_PROJECT_ID=reinoro
VITE_FIREBASE_AUTH_DOMAIN=reinoro.firebaseapp.com
VITE_FIREBASE_API_KEY=your-firebase-web-api-key
VITE_FIREBASE_STORAGE_BUCKET=reinoro.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Keep false to use the backend Firestore OTP/password flow.
# Set true only when Firebase Auth phone login, reCAPTCHA domains, and Firestore rules are deployed.
VITE_FIREBASE_CLIENT_AUTH=false

# Optional backend URL. Leave blank for Vite proxy/local relative /api.
VITE_BACKEND_URL=
